<% field_errors = event.errors.where(field) %>
<% if field_errors.any? %>
    <p class="bg-red-300 text-red-900 p-2 rounded text-sm font-semibold">
        <%= field_errors.first.full_message %>
    </p>
<% end %>
